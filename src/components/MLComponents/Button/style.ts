import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    button: {
        backgroundColor: "#3483FC",
        width: "90%",
        height: 25,
        borderRadius: 5,
        alignItems: "center",
        justifyContent: "center"
    },
    buttonText: {
        fontSize: 11,
        color: "#FFF",
    },
});
