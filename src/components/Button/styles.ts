import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    btn: {
        alignItems: "center",
        justifyContent: "center",
        borderRightWidth: 2,
        borderTopWidth: 1,
        borderLeftWidth: 1,
        borderBottomWidth: 3,
        width: "30%",
    }
});